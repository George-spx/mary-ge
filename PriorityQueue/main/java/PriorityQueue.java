

public class PriorityQueue<T>{

  
  /**
   * check if the queue is empty
   */
  public boolean isEmpty(){

  }
  
 /**
  * inserts the element in the priority queue in the right position
  * @param element the element to be inserterd
  * @param priority the priority of the element
  */ 
  public void insertWithPriority(T element, int priority){
  
  }

  /**
   * pops the element in the queue with the highest priority
   * @return the first element in the queue
   */
  public T pop(){
  
  }
 
 /**
  * returns the element with highest priority without popping it
  * @return the element with the highest priority
  */ 
  public T peek(){
  
  }

  /**
   * returns the number of elements in the queue
   * @return the numbers of elements in the queue
   */
  public int size(){
  
  }
}
